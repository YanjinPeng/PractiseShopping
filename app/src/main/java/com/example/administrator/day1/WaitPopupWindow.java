package com.example.administrator.day1;


/**
 *  PopupWindow弹窗
 *  WaitPopupWindow waitPopupWindow = new waitPopupWindow(Home_mc_activity.this,listener);
 *  waitPopupWindow.showAtLocation(getActivity().this.findViewById
 *  (R.id.根部局id), Gravity.CENTER | Gravity.CENTER, 0, 0);
 *	调用上面的方法即可弹出输入支付密码界面
 * **/
public class WaitPopupWindow {
}
